pspsps makeAdder (offset) {

  pspsps add (x, y)  {
    x + y + offset
  }

  add
}

staticat adder = makeAdder(2); # declare a new staticat variable (const) called adder, which is the result of calling makeAdder 

meow(adder(10, 5)) # print 17